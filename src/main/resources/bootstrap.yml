#Server port
server:
  port: 2001

#Git repo location.
spring:
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/khames05/easyms-config-repo
          clone-on-start: true